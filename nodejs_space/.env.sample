
# Database
DATABASE_URL=postgresql://user:password@localhost:5432/letip_leads

# Server
PORT=3000

# Abacus AI API Key (for LLM)
ABACUSAI_API_KEY=your_api_key_here

# API Secrets
# The system automatically loads API secrets from the configured secrets file
# Required secrets: hunter.io api_key, abstractapi api_key, telegram bot_token
